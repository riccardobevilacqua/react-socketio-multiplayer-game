{
  "name": "react-socketio-multiplayer-game",
  "version": "0.1.0",
  "description": "React and socket.io multiplayer game",
  "main": "index.ts",
  "scripts": {
    "start:dev": "nodemon",
    "start": "node ./dist",
    "build": "npx tsc && cd frontend && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-postbuild": "tsc && cd frontend && npm install && npm run build && cd .. && rm -rf ./frontend && rm -rf ./backend && mv ./build/* ."
  },
  "keywords": [
    "react",
    "socketio"
  ],
  "author": "Riccardo Bevilacqua <riccardo.bevilacqua@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^2.0.6",
    "typescript": "^3.9.7"
  },
  "dependencies": {
    "@types/express": "^4.17.8",
    "express": "^4.17.1",
    "socket.io": "^2.3.0"
  }
}